# Change Analysis Configuration

# LLM Configuration
# Set to true to enable AWS Bedrock for LLM-powered summarization
# When false, uses deterministic local fallback summarizer
USE_BEDROCK=false

# AWS Bedrock Model ID
# Default: anthropic.claude-3-5-sonnet-20240620-v1:0
# Ensure you have AWS credentials configured (via ~/.aws/credentials or env vars)
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20240620-v1:0

# AWS Credentials (if not using ~/.aws/credentials)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-east-1

# Logging
# LOG_LEVEL=INFO
